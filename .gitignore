# Heavy artifacts live on scratch + move via Globus — NOT git.
*.sif
*.duckdb
*.duckdb.wal
__pycache__/
*.pyc
.ipynb_checkpoints/
# Per-agent local Claude Code settings (permission posture is machine-local, not shared across clusters).
.claude/
data/interim/
# Fetched antigen/reference coordinate files — heavy + reproducible via
# scripts/stage1_inputs/fetch_antigen_templates.sh; the git-tracked source of
# truth is data/raw/antigen/antigen_templates.md (IDs + provenance).
data/raw/antigen/coords/
results/**/samples/
results/**/*.pdb
results/**/*.cif
# The variant ledger binary is git-ignored (unmergeable across agents);
# the git-shared form is db/exports/*.csv + coordination/manifests/*.tsv.
